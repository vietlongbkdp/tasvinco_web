import styled from 'styled-components';

export const NumberMainStyles = styled.div`
  width: 80%;
  margin-inline: auto;
  padding-bottom: 80px;
`;

export const ContentStyles = styled.div`
  font-size: 30px;
  font-weight: 900;
  color: #333132;
  text-align: center;
  text-transform: uppercase;
  margin-bottom: 120px;
`;

export const StylesNumber = styled.div`
  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 60px;
`;

export const ImagesStyles = styled.img`
  width: 20px;
  text-align: center;
  filter: brightness(0) sepia(1) saturate(10000%) hue-rotate(-50deg) brightness(0.5);
`;
